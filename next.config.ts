import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['localhost'], // Add other domains if needed (e.g., production URL)
  },
};

export default nextConfig;
